import randomColor from 'randomcolor';

export default function Button(props) {
  const randomInteger
  return (
    <button type="input" onClick= {() => {
    }}>
      Generate
    </button>
  );
}
